services:
  - type: web
    name: tiktok-clips-project
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      # تثبيت FFmpeg في بيئة Render
      apt-get update && apt-get install -y ffmpeg
    startCommand: gunicorn app:app
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
